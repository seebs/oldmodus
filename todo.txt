Preferences
	add speed setting
Apple store
	Verify conversion
Android store
	Create account (?)
Cascade
	Sometimes touching one square alters another (!??)
Android permissions issues
	find out how to rebuild app without permissions
	... and exclude the code that wanted them

POST-RELEASE:

Lite mode?
	Limited version (fewer features/settings)
Sound
	possibly allow random selection between notes for intermediate colors

VERSION 2:

Line mode:
	More string art
Sound
	primary/secondary distinction for at least some modes
Vector
	adjust total velocity, not velocity in each direction
