Sound
	make the octave-choosing smarter/prettier
	possibly allow random selection between notes for intermediate colors
Drops
	Apparently not following whole path?
Benchmark
	improve settings usage
	compare on a couple of devices
Preferences
	add speed setting
	allow sound selection
Touch
	error from dist() thinking it didn't get a valid object for the
	calculation on 135 (determining distance of touch)
Apple store
	Verify conversion
Android store
	Create account (?)

POST-RELEASE:

Lite mode?
	Limited version (fewer features/settings)

VERSION 2:

Line mode:
	More string art
Sound
	primary/secondary distinction for at least some modes
Vector
	adjust total velocity, not velocity in each direction
